import { MemberSincePipe } from './member-since.pipe';

describe('MemberSincePipe', () => {
  it('create an instance', () => {
    const pipe = new MemberSincePipe();
    expect(pipe).toBeTruthy();
  });
});
